/**
 * credentials.example.h - WiFi credentials template
 * 
 * SETUP INSTRUCTIONS:
 * 1. Copy this file to credentials.h
 * 2. Replace the placeholder values with your actual WiFi credentials
 * 3. credentials.h is gitignored, so your secrets won't be committed
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

#define WIFI_SSID     "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#endif // CREDENTIALS_H
